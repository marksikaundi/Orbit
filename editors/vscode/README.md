# Orbit Terminal for Cursor / VS Code

Adds **Open in Orbit Terminal** to the explorer and editor context menus.

Installed automatically by:

```bash
orbit ide-setup
```

Then reload the editor window.

Set `orbit.path` in settings if the `orbit` command is not on PATH.
